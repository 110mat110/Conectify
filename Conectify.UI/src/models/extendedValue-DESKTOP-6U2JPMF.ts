export interface BaseInputType {
    id: string;
    type: string;
    sourceId: string;
    name: string;
    unit: string;
    stringValue: string;
    numericValue: number;
    timeCreated: number;  
}